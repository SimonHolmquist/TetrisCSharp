namespace TetrisCSharp.Application.UseCases;

// Placeholders para Fase 1: comandos/acciones de la aplicación
public sealed record Tick;
public sealed record MoveLeft;
public sealed record MoveRight;
public sealed record RotateCW;
public sealed record RotateCCW;
public sealed record SoftDrop;
public sealed record HardDrop;
public sealed record Pause;
public sealed record Resume;
public sealed record StartGame;
public sealed record CommitLock;
public sealed record ClearLinesAndLevelUp;
